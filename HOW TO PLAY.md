# 怎麼玩 LittleFlowerBot 🎮

歡迎！本文件介紹如何在 Line 上跟 **LittleFlowerBot** 一起玩遊戲。

## 開始之前

1. 將 `LittleFlowerBot` 加為 Line 好友
2. 想跟朋友對戰：把 Bot 拉進**群組**或**多人聊天室**
3. 想自己玩：直接私訊 Bot 也行（但對戰類遊戲需要兩位玩家）

> ⚠️ 玩**海戰棋**時，每位參賽者都必須先把 Bot 加為好友（Bot 需要私訊你佈置船艦與盤面）。

## 通用指令

在任何聊天視窗（個人 / 群組 / 聊天室）輸入：

| 指令 | 說明 |
|---|---|
| `玩遊戲` | 顯示遊戲選單（快速回覆按鈕） |
| `小遊戲說明` | 列出所有可用指令 |
| `我的戰績` | 查看自己的勝負紀錄 |
| `我認輸了` | 投降，結束當前遊戲 |

---

## 🎲 遊戲清單

### 1. 猜數字

**啟動**：輸入 `玩猜數字`

**規則**：Bot 會在 0~100 之間挑一個數字，你輸入數字猜，Bot 會縮小範圍直到你猜中。

**輸入**：純數字

**範例**：
```
你：玩猜數字
Bot：猜 0 - 100
你：50
Bot：猜 50 - 100
你：75
Bot：猜 50 - 75
你：63
Bot：猜對了! 總共 3 次
```

---

### 2. 井字遊戲（Tic-Tac-Toe）

**啟動**：輸入 `玩井字遊戲`

**規則**：3×3 棋盤，先連成一線（橫、直、斜）獲勝。雙人輪流。

**輸入格式**：`<列><行>`
- 列：`1` ~ `3`（由上往下）
- 行：`a` ~ `c`（由左往右）

**範例**：
```
你：玩井字遊戲
Bot：[顯示空棋盤]
玩家A：2b   ← 中央
玩家B：1a   ← 左上角
玩家A：3c   ← 右下角
...
```

---

### 3. 五子棋（Gomoku）

**啟動**：輸入 `玩五子棋`

**規則**：15×15 棋盤，先連成五子一線（橫、直、斜）獲勝。雙人輪流。

**輸入格式**：`<列><行>`
- 列：`1` ~ `15`
- 行：`a` ~ `o`

**範例**：
```
你：玩五子棋
Bot：[顯示空棋盤]
玩家A：8h    ← 中央
玩家B：8i
玩家A：9h
...
```

---

### 4. 象棋（中國象棋）

**啟動**：輸入 `玩象棋`

**規則**：標準中國象棋規則，雙人輪流。

**輸入格式**：`<起點>><終點>`
- 座標：`<列><行>`，列 `1` ~ `10`、行 `a` ~ `i`
- 用 `>` 表示「移動到」

**範例**：
```
你：玩象棋
Bot：[顯示棋盤]
玩家A：7a>6a    ← 把 7a 的棋子移到 6a
玩家B：4a>5a
...
```

**特殊回覆**：
- 走錯子：`不是你的棋子`
- 走法不合規則：`走法有誤`

---

### 5. 海戰棋（Battleship）

**啟動**：輸入 `玩海戰棋`

**規則**：兩位玩家各自佈置 5 艘船艦在 10×10 棋盤上，輪流攻擊對方座標，先把對方所有船艦擊沉者獲勝。

**流程分三階段**：

#### 階段一：加入遊戲
```
你：玩海戰棋
Bot：【海戰棋】輸入++參加遊戲（需要2位玩家）
玩家A：++
Bot：玩家A 已加入遊戲
玩家B：++
Bot：兩位玩家就位！請開始佈置船艦
```

Bot 會**私訊**兩位玩家提示佈置指令。

> ⚠️ 若你尚未加 Bot 為好友，Bot 會回覆「請先加入 Bot 好友才能參加海戰棋！」

#### 階段二：佈置船艦（私訊 Bot）

每位玩家私訊 Bot 放置 5 艘船艦：

| 船艦 | 長度 |
|---|---|
| 航母 | 5 格 |
| 戰艦 | 4 格 |
| 巡洋艦 | 3 格 |
| 潛艇 | 3 格 |
| 驅逐艦 | 2 格 |

**指令格式**：`放置 <船艦名稱> <座標> <方向>`
- 座標：`<行><列>`，行 `a` ~ `j`、列 `1` ~ `10`
- 方向：`橫` 或 `直`

**範例（私訊 Bot）**：
```
你：放置 航母 a1 橫     ← 從 a1 往右延伸 5 格（a1~a5 那一橫排）
Bot：[私訊回傳你自己的盤面圖]
你：放置 戰艦 c3 直     ← 從 c3 往下延伸 4 格
你：放置 巡洋艦 e5 橫
你：放置 潛艇 g2 直
你：放置 驅逐艦 j9 橫
Bot：佈置完成！等待對手完成佈置
```

兩位都佈置完成後，Bot 會在群組宣布開始戰鬥。

#### 階段三：互攻

在群組輪流輸入要攻擊的座標：

**指令格式**：`<行><列>`，例如 `e5`

```
玩家A：e5
Bot：玩家A 攻擊 e5 → 命中！
玩家B：a1
Bot：玩家B 攻擊 a1 → 未命中
玩家A：e6
Bot：玩家A 攻擊 e6 → 命中！擊沉了巡洋艦！
...
玩家A：c4
Bot：玩家A 勝利！所有敵艦已被擊沉！
```

Bot 會在每次攻擊後**私訊**雙方更新後的盤面圖（你看得到自己被攻擊的格子，也看得到對你發起攻擊後的雷達圖）。

---

## 常見問題

**Q：可以同時開多個遊戲嗎？**
A：同一個聊天視窗（群組 / 私訊）一次只能有一個進行中的遊戲。要換遊戲先輸入新的 `玩XX` 指令會自動覆蓋上一個。

**Q：怎麼結束遊戲？**
A：輸入 `我認輸了` 即可。

**Q：戰績會記錄嗎？**
A：目前 `我的戰績` 主要顯示**五子棋、井字遊戲**的勝負紀錄。

**Q：私訊裡可以玩雙人遊戲嗎？**
A：可以玩單人類（猜數字），但棋類需要兩位玩家在同一個聊天視窗。

---

開心玩 🎉
